package com.example.constaweather.data.db

/*
 So since the database entity has the info for f and c we have decided to separate it into 2 parts.
 for imperial and metric
    So when we use the dao we can select witch one we want to extract
 */